{
  "name": "@simforge/evaluation",
  "version": "0.1.0-rc.45",
  "description": "WS5 EvalHarness \u2014 frozen policy evaluation protocol: versioned suite (policy-eval-suite.v1), deterministic env server over the suite entries, and suite-hash-keyed report inputs. The Python runner lives under qualification/policy-eval-runner.",
  "repository": {
    "type": "git",
    "url": "https://github.com/SimForgeinc/UniScenarios",
    "directory": "packages/evaluation"
  },
  "license": "Apache-2.0",
  "type": "module",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "bin": {
    "simforge-evaluation-build-suite": "./dist/build-suite-cli.js",
    "simforge-evaluation-server": "./dist/eval-server.js"
  },
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "default": "./dist/index.js"
    }
  },
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsup src/index.ts src/build-suite-cli.ts src/eval-server.ts src/examiner/extractor/cli.ts --format esm --dts --sourcemap --clean --out-dir dist",
    "typecheck": "tsc --noEmit",
    "test": "vitest run"
  },
  "dependencies": {
    "@simforge/compiler": "workspace:*",
    "@simforge/engine": "workspace:*",
    "@simforge/scenario": "workspace:*",
    "@simforge/training-env": "workspace:*",
    "zod": "^4.4.3"
  },
  "devDependencies": {
    "@types/node": "^22.10.0",
    "tsup": "^8.5.0",
    "tsx": "^4.20.0",
    "typescript": "^5.6.0",
    "vitest": "^3.0.0"
  }
}
