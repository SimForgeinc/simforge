{
  "name": "@uniscenarios/policy-eval",
  "version": "0.1.0-rc.45",
  "description": "WS5 EvalHarness — frozen policy evaluation protocol: versioned suite (policy-eval-suite.v1), deterministic env server over the suite entries, and suite-hash-keyed report inputs. The Python runner lives under tools/policy-eval-runner.",
  "repository": {
    "type": "git",
    "url": "https://github.com/SimForgeinc/UniScenarios",
    "directory": "packages/policy-eval"
  },
  "license": "Apache-2.0",
  "type": "module",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "bin": {
    "policy-eval-build-suite": "./dist/build-suite-cli.js",
    "policy-eval-server": "./dist/eval-server.js"
  },
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "default": "./dist/index.js"
    }
  },
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsup src/index.ts src/build-suite-cli.ts src/eval-server.ts --format esm --dts --sourcemap --clean --out-dir dist",
    "typecheck": "tsc --noEmit",
    "test": "vitest run"
  },
  "dependencies": {
    "zod": "^4.4.3"
  },
  "devDependencies": {
    "@types/node": "^22.10.0",
    "typescript": "^5.6.0",
    "vitest": "^3.0.0"
  }
}
