import { NextResponse } from "next/server";
import { ReserveUniScenarioSimulationPreviewSchema } from "@/app/lib/uniscenario/contracts";
import { getCurrentSimulationPreview, reserveSimulationPreview } from "@/app/lib/uniscenario/simulation-preview-store";
import { readJson, requireUniScenarioContext, requireUniScenarioMutableDocumentContext, requireUniScenarioMutationOrigin, UNISCENARIO_PRIVATE_CACHE_HEADERS } from "@/app/lib/uniscenario/http";
type Context = { params: Promise<{ documentId: string }> };
export async function GET(_request: Request, route: Context) { const auth=await requireUniScenarioContext(); if(auth.response)return auth.response; const {documentId}=await route.params; const value=await getCurrentSimulationPreview(auth.context,documentId); return value?NextResponse.json(value,{headers:UNISCENARIO_PRIVATE_CACHE_HEADERS}):NextResponse.json({error:"simulation_preview_not_found"},{status:404}); }
export async function POST(request: Request, route: Context) { const origin=requireUniScenarioMutationOrigin(request); if(origin)return origin; const auth=await requireUniScenarioContext(); if(auth.response)return auth.response; const {documentId}=await route.params; const access=await requireUniScenarioMutableDocumentContext(auth.context,documentId,"mutateContent"); if(access.response)return access.response; const parsed=ReserveUniScenarioSimulationPreviewSchema.safeParse(await readJson(request)); if(!parsed.success)return NextResponse.json({error:"invalid_simulation_preview"},{status:400}); const value=await reserveSimulationPreview(auth.context,documentId,parsed.data); return value?NextResponse.json(value):NextResponse.json({error:"stale_simulation_preview"},{status:409}); }
